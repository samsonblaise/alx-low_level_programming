#include <stdio.h>

/**
 * main - Prints entry point
 * Return: Alwyas 0 (successful)
 */

int main(void)
{
	char c;

	char d;

	c = 'a';
	d = 'b';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(d <= 'z') {
			putchar(c);
			d++;
		}
	putchar('\n');
	return (0);
}
